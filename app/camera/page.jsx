"use client";
import React from "react";
import { motion } from "framer-motion";
const Camera = () => {
  return (
    <motion.div layoutId="camera-page" className="w-full h-full bg-black p-3">
      Camera
    </motion.div>
  );
};

export default Camera;
